(function () {
  // Support screen JS bootstrap (reserved for future interactions).
  // Currently no interactive elements beyond the hyperlink.
  // You can add analytics or event hooks here if needed.
})();
