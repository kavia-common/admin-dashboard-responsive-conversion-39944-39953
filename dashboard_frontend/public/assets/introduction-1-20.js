(function(){
  // Placeholder for any interactions (e.g., link tracking or image lazy-loading hooks).
  // Keeping minimal as the screen is mostly static content.
  document.addEventListener('DOMContentLoaded', function(){
    // Example: enhance external-style links if needed in future.
    const links = document.querySelectorAll('a.link');
    links.forEach(a => {
      a.addEventListener('click', () => {
        // no-op placeholder
      });
    });
  });
})();
